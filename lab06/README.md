Create new Container: $sudo docker-compose up --build -d
Check Container: $sudo docker ps
Check container of project: $sudo docker-compose ps
Stop Container: $sudo docker-compose down

Restart: $sudo docker-compose up